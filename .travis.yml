language: generic

deploy:
  skip_cleanup: true
  provider: script
  script: curl -o- https://raw.githubusercontent.com/creationix/nvm/v0.33.1/install.sh | bash
       && nvm install node
       && npm install -g now
       && now -t $NOW_TOKEN -a sparkbrooklovessoftware
